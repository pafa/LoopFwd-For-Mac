# Gate C Mac repo default for drop-dir runners / auto-kick.
# Sourced only when LOOPFWD_MAC_REPO is unset/empty.
# Prefer hub-start worktree when origin/main lacks GATE_C_SAME_REQUEST_ID_PROOF.
# Does NOT git-switch any checkout (including dirty primary). Env default only.
# shellcheck shell=bash
export LOOPFWD_MAC_REPO="${HOME}/Documents/Cursor/LoopFwd-worktrees/mac-gate-c-hub-start-641d"
